本文档负责对AutoCoding工具的说明

======================================================
1、 ./conf/config.int
工具主要配置文件，用于配置基本信息（键可以重复，以最后一个为准）
例如：
#作者                    ——#号后跟注释
@AUTOR      ——@后为键
HDZhang123  ——为值
@@          ——文件结束符
======================================================

======================================================
2、 ./template/service.tmp
服务配置文件（一个键对应一个值，键可以重复，以第一个为准）
例如：
@AUTOR@      ——替换关键字
HDZhang      ——替换值
======================================================

======================================================
3. head_template.tmp
头文件模板
例如：
@AUTOR@      ——替换处对应键
======================================================

======================================================
4. src_template.tmp
源文件模板
例如：
@CLASSNAME@      ——替换处对应键
======================================================