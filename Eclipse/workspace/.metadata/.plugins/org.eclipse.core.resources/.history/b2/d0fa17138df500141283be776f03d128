本文档负责对AutoCoding工具的说明

======================================================
1、 ./conf/config.int
	工具主要配置文件
		(1)配置基本信息（键可以重复，以最后一个为准）
		(2)配置需要加载的服务（以 “@ServiceTemplate” 为关键字，需要加载多少服务，就配置多少）
		(3)配置需要加载的代码模板（以“@CodeTemplate”为关键字，需要加载多少代码模板，就配置多少）
例如：
#作者                    ——#号后跟注释
@AUTOR      ——@后为键
HDZhang123  ——为值
@@          ——文件结束符
======================================================

======================================================
2、 ./template/service.tmp
服务配置文件（一个键对应一个值，键可以重复，以第一个为准）
例如：
@AUTOR@      ——替换关键字
HDZhang      ——替换值
======================================================

======================================================
3. head_template.tmp
头文件模板
例如：
@AUTOR@      ——替换处对应键
======================================================

======================================================
4. src_template.tmp
源文件模板
例如：
@CLASSNAME@      ——替换处对应键
======================================================